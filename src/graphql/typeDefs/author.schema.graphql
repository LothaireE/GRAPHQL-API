# type Author {
#     id: ID!
#     firstName: String!
#     lastName: String!
#     books: [Book]
# }
type Author {
    id: ID!
    firstName: String!
    lastName: String!
    books: [Book!]! # each author can han multiple book so book.resolver
}

input AuthorInput {
    firstName: String!
    lastName: String!
}

input UpdateAuthorInput {
    id: ID!
    firstName: String!
    lastName: String!
    # updateAt: String
}
